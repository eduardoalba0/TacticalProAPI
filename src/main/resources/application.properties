spring.application.name=TacticalPro
# Porta em que a aplica??o Spring Boot ser? executada.
server.port=8080
# Cuidado com caracteres especiais na senha, como '#', etc.
# Quando usar o pooler de conex?es, incluir o par?metro 'prepareThreshold=0' para evitar problemas de prepara??o de consultas.
spring.datasource.url=${DATABASE_URL}
# Esquema de cria??o do banco de dados: 'create' para criar as tabelas a cada execu??o, 'update' para atualizar o esquema.
spring.jpa.hibernate.ddl-auto=validate
# Exibe as consultas SQL no console para depura??o.
spring.jpa.show-sql=true
# Define o dialeto do Hibernate para PostgreSQL, garantindo compatibilidade com as funcionalidades espec?ficas do banco de dados.
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT}

